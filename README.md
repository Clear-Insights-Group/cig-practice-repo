# cig-practice-repo

A simple static website for Clear Insights Group.

## Files

- `index.html` — landing page
- `styles.css` — styling

## Local preview

Open `index.html` in a browser, or serve it locally:

```sh
python -m http.server 8000
```

Then visit http://localhost:8000.
